{
  "name": "root",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.6.2"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=7"
  },
  "scripts": {
    "fmt": "nx run-many --target=fmt --parallel=5",
    "lint": "nx run-many --target=lint --parallel=5"
  },
  "dependencies": {
    "nx": "^16.1.4"
  }
}
